import java.util.Arrays;

class Solution {
    public static void sortArray(int arr[]) {
        
        Arrays.sort(arr);
        
        // No need to print anything
        // The array gets sorted in-place
    }
}
